# Base URL of the FastAPI service in vivid-backend.
# Server-side only on purpose: the browser never talks to the backend directly,
# it goes through the route handlers in app/api.
VIVID_API_BASE_URL=http://localhost:8000
